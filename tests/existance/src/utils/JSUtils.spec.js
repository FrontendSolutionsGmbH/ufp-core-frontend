
                import JSUtils from 'utils/JSUtils'

                describe('Class JSUtils', () => {
                    it('Should Exist', () => {
                        expect(JSUtils).to.exist
                    })
                })