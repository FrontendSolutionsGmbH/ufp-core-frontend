
                import DeepGetSet from 'utils/DeepGetSet'

                describe('Class DeepGetSet', () => {
                    it('Should Exist', () => {
                        expect(DeepGetSet).to.exist
                    })
                })