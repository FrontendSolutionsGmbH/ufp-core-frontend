
                import ArrayUtils from 'utils/ArrayUtils'

                describe('Class ArrayUtils', () => {
                    it('Should Exist', () => {
                        expect(ArrayUtils).to.exist
                    })
                })