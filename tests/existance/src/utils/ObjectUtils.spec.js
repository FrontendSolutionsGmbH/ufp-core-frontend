
                import ObjectUtils from 'utils/ObjectUtils'

                describe('Class ObjectUtils', () => {
                    it('Should Exist', () => {
                        expect(ObjectUtils).to.exist
                    })
                })
