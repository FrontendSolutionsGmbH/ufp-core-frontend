
                import UfpMiddlewareConfiguration from 'middleware/UfpMiddlewareConfiguration'

                describe('Class UfpMiddlewareConfiguration', () => {
                    it('Should Exist', () => {
                        expect(UfpMiddlewareConfiguration).to.exist
                    })
                })
