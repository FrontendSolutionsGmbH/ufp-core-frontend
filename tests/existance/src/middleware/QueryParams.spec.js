
                import QueryParams from 'middleware/QueryParams'

                describe('Class QueryParams', () => {
                    it('Should Exist', () => {
                        expect(QueryParams).to.exist
                    })
                })
