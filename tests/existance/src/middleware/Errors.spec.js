
                import Errors from 'middleware/Errors'

                describe('Class Errors', () => {
                    it('Should Exist', () => {
                        expect(Errors).to.exist
                    })
                })
