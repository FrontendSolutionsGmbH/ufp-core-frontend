
                import IntlReducer from 'modules/intl/model/IntlReducer'

                describe('Class IntlReducer', () => {
                    it('Should Exist', () => {
                        expect(IntlReducer).to.exist
                    })
                })
