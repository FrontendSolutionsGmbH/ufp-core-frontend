
                import Router from 'modules/react-redux-hash-router3/view/components/Router'

                describe('Class Router', () => {
                    it('Should Exist', () => {
                        expect(Router).to.exist
                    })
                })
