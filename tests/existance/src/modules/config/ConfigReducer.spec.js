
                import ConfigReducer from 'modules/config/ConfigReducer'

                describe('Class ConfigReducer', () => {
                    it('Should Exist', () => {
                        expect(ConfigReducer).to.exist
                    })
                })