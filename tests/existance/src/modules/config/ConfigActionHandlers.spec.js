
                import ConfigActionHandlers from 'modules/config/ConfigActionHandlers'

                describe('Class ConfigActionHandlers', () => {
                    it('Should Exist', () => {
                        expect(ConfigActionHandlers).to.exist
                    })
                })