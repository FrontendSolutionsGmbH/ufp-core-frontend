
                import MenuReducer from 'modules/menu/MenuReducer'

                describe('Class MenuReducer', () => {
                    it('Should Exist', () => {
                        expect(MenuReducer).to.exist
                    })
                })