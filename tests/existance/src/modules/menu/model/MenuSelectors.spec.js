
                import MenuSelectors from 'modules/menu/model/MenuSelectors'

                describe('Class MenuSelectors', () => {
                    it('Should Exist', () => {
                        expect(MenuSelectors).to.exist
                    })
                })
