
                import DefaultMenuRenderer from 'modules/menu/components/DefaultMenuRenderer'

                describe('Class DefaultMenuRenderer', () => {
                    it('Should Exist', () => {
                        expect(DefaultMenuRenderer).to.exist
                    })
                })
