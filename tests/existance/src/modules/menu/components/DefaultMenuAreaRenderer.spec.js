
                import DefaultMenuAreaRenderer from 'modules/menu/components/DefaultMenuAreaRenderer'

                describe('Class DefaultMenuAreaRenderer', () => {
                    it('Should Exist', () => {
                        expect(DefaultMenuAreaRenderer).to.exist
                    })
                })
