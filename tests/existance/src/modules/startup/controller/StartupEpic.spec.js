
                import StartupEpic from 'modules/startup/controller/StartupEpic'

                describe('Class StartupEpic', () => {
                    it('Should Exist', () => {
                        expect(StartupEpic).to.exist
                    })
                })
