
                import UfpSetup from 'core/UfpSetup'

                describe('Class UfpSetup', () => {
                    it('Should Exist', () => {
                        expect(UfpSetup).to.exist
                    })
                })
