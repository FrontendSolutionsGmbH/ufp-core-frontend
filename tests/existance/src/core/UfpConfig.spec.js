
                import UfpConfig from 'core/UfpConfig'

                describe('Class UfpConfig', () => {
                    it('Should Exist', () => {
                        expect(UfpConfig).to.exist
                    })
                })
