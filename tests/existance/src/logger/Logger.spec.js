
                import Logger from 'logger/Logger'

                describe('Class Logger', () => {
                    it('Should Exist', () => {
                        expect(Logger).to.exist
                    })
                })
